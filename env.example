# Application
NODE_ENV=development
APP_PORT=3000
DEV_MODE=true

# JWT Authentication
JWT_SECRET_KEY=your-secret-key-change-this-in-production

# Database - Development (individual variables)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=boilerplate_db
DB_USER=postgres
DB_PASSWORD=password
DB_DIALECT=postgres

# Database - Production (connection string)
# Use DATABASE_URL in production instead of individual variables
# DATABASE_URL=postgresql://user:password@host:port/database
